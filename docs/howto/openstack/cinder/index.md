# Block storage (Cinder)

**[Cinder](https://docs.openstack.org/cinder/)** is an important OpenStack component that provides persistent block storage to [virtual servers](../nova/index.md).

{{brand}} uses Cinder to provide unencrypted persistent block storage devices ("volumes") to virtual servers by default, with [encrypted volumes](encrypted-volumes.md) available as an option.
